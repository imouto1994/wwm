# Only needed for Tina Cloud (production CMS editing). Not required for local dev or static export.
# NEXT_PUBLIC_TINA_CLIENT_ID=***
# TINA_TOKEN=***
# NEXT_PUBLIC_TINA_BRANCH=main

# Set during build:gh-pages to prefix all routes for GitHub Pages subpath hosting.
# NEXT_PUBLIC_BASE_PATH=/wwm
